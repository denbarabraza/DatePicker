export enum WeekendStatusEnum {
  WithWeekEnds = 'with',
  WithoutWeekEnds = 'without',
}

export enum InputEnum {
  Task = 'task',
  Date = 'date',
}

export enum SelectEnum {
  Month = 'day',
  Year = 'currency',
}
